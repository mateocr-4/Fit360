{
  "name": "fit360",
  "version": "1.1.0",
  "description": "Fit360 - Fitness & Nutrition Tracker para iOS (Sideloadly)",
  "main": "index.html",
  "scripts": {
    "build": "node build_dist.js",
    "cap:add:ios": "npx cap add ios",
    "cap:sync": "node build_dist.js && npx cap sync ios",
    "cap:open": "npx cap open ios",
    "cap:build": "node build_dist.js && npx cap sync ios && npx cap open ios",
    "prepare-sideload": "node build_dist.js && echo '✅ www/ listo. Ahora ejecuta: npx cap sync ios (en Mac con Xcode)'"
  },
  "author": "Mateo",
  "license": "MIT",
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2",
    "@followathletics/capacitor-healthkit": "^1.3.7"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
